# Market symbols
SP500_SYMBOL=^GSPC
TOPIX_SYMBOL=1306.T
NIKKEI_SYMBOL=^N225
NIFTY50_SYMBOL=^NSEI
ORUKAN_SYMBOL=ACWI
ORUKAN_JPY_SYMBOL=ACWI
ORUKAN_JPY_FX_SYMBOL=JPY=X
SP500_FUND_SYMBOL=03311187.T

# Optional NAV API bases (set when you host your own NAV endpoints)
SP500_NAV_API_BASE=
TOPIX_NAV_API_BASE=
NIKKEI_NAV_API_BASE=
NIFTY50_NAV_API_BASE=

# Macro data (FRED)
FRED_API_KEY=

# Fallback controls (0=disable, 1=enable)
# ローカル検証: 疑似データのみ（真偽値として解釈される 1/true/yes/on を指定）
# BACKTEST_ALLOW_FALLBACK=1
# SP500_ALLOW_SYNTHETIC_FALLBACK=1
# TOPIX_ALLOW_SYNTHETIC_FALLBACK=1
# NIKKEI_ALLOW_SYNTHETIC_FALLBACK=1
# NIFTY50_ALLOW_SYNTHETIC_FALLBACK=1

# 本番想定: 実データ優先 + 失敗時は疑似へフォールバック（指数ごとに許可を調整）
BACKTEST_ALLOW_FALLBACK=1
SP500_ALLOW_SYNTHETIC_FALLBACK=1
TOPIX_ALLOW_SYNTHETIC_FALLBACK=1
NIKKEI_ALLOW_SYNTHETIC_FALLBACK=1
NIFTY50_ALLOW_SYNTHETIC_FALLBACK=1

# Vercel KV (Push token storage)
KV_REST_API_URL=
KV_REST_API_TOKEN=

# Vercel Cron security secret
CRON_SECRET=
